<?php include 'header.php'; ?>

    <title>Dog Lap Day Calculator 2026 - National Dog Day Fun Calculator | Thiyagi.com</title>
    <meta name="description" content="Dog lap day calculator 2026 - calculate your dog's lap day frequency based on size, breed, and personality. Fun National Dog Day calculator with cute dog statistics.">
    <meta name="keywords" content="dog lap day calculator 2026, national dog day calculator, dog size calculator, dog breed calculator, pet lap time">
    <meta name="author" content="Thiyagi">
        
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="Dog Lap Day Calculator 2026 - National Dog Day Fun Calculator">
    <meta property="og:description" content="Calculate your dog's lap day frequency based on size, breed, and personality with our fun dog calculator.">
    <meta property="og:url" content="https://www.thiyagi.com/dog-lap-day-calculator">
    <meta property="og:type" content="website">
    <meta property="og:image" content="https://www.thiyagi.com/nt.png">
    
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Dog Lap Day Calculator 2026 - National Dog Day Fun Calculator">
    <meta name="twitter:description" content="Calculate your dog's lap day frequency with our fun National Dog Day calculator.">
    <meta name="twitter:image" content="https://www.thiyagi.com/nt.png">
    
    <!-- Canonical URL -->
    
    <!-- Tailwind CSS -->

<style>
    .gradient-bg {
        background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }
    .dog-card {
        transition: all 0.3s ease;
        border-left: 4px solid #f59e0b;
    }
    .dog-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        border-left-color: #d97706;
    }
    .fade-in {
        animation: fadeIn 0.5s ease-in-out;
    }
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .dog-paw {
        animation: pawBounce 2s ease-in-out infinite;
    }
    @keyframes pawBounce {
        0%, 100% { transform: translateY(0) rotate(0deg); }
        50% { transform: translateY(-5px) rotate(5deg); }
    }
    .tail-wag {
        animation: tailWag 1s ease-in-out infinite;
    }
    @keyframes tailWag {
        0%, 100% { transform: rotate(-10deg); }
        50% { transform: rotate(10deg); }
    }
    .dog-size-small { border-left-color: #10b981; background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%); }
    .dog-size-medium { border-left-color: #3b82f6; background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%); }
    .dog-size-large { border-left-color: #8b5cf6; background: linear-gradient(135deg, #f3e8ff 0%, #e9d5ff 100%); }
    .dog-size-giant { border-left-color: #f59e0b; background: linear-gradient(135deg, #fffbeb 0%, #fef3c7 100%); }
    .personality-calm { background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%); }
    .personality-playful { background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); }
    .personality-energetic { background: linear-gradient(135deg, #fecaca 0%, #fca5a5 100%); }
    .personality-cuddly { background: linear-gradient(135deg, #f3e8ff 0%, #e9d5ff 100%); }
</style>

<!-- JSON-LD Structured Data -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "Dog Lap Day Calculator 2026",
  "description": "Fun calculator to determine your dog's lap day frequency based on size, breed, personality, and owner preferences. Perfect for National Dog Day celebrations.",
  "url": "https://www.thiyagi.com/dog-lap-day-calculator",
  "applicationCategory": "EntertainmentApplication",
  "operatingSystem": "Any",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "USD"
  },
  "creator": {
    "@type": "Organization",
    "name": "Thiyagi.com"
  }
}
</script>

<body class="bg-gray-50">
    <!-- Header -->
    <header class="gradient-bg shadow-lg">
        <div class="max-w-6xl mx-auto px-4 py-6">
            <div class="flex items-center justify-center">
                <div class="flex items-center space-x-4">
                    <div class="bg-white p-3 rounded-full shadow-lg">
                        <i class="fas fa-dog text-2xl text-amber-600 dog-paw" aria-hidden="true"></i>
                    </div>
                    <div>
                        <h1 class="text-3xl font-bold text-white">Dog Lap Day Calculator</h1>
                        <p class="text-amber-100">Calculate your furry friend's lap time preferences</p>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Breadcrumb -->
    <nav class="bg-white border-b" aria-label="Breadcrumb">
        <div class="max-w-6xl mx-auto px-4 py-3">
            <ol class="flex items-center space-x-2 text-sm">
                <li><a href="/" class="text-gray-500 hover:text-gray-700">Home</a></li>
                <li><i class="fas fa-chevron-right text-gray-400" aria-hidden="true"></i></li>
                <li class="text-gray-900 font-medium">Dog Lap Day Calculator</li>
            </ol>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-6xl mx-auto px-4 py-8">
        <!-- Calculator Tool -->
        <section class="bg-white rounded-xl shadow-lg p-8 mb-8">
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-amber-100 rounded-full mb-4">
                    <i class="fas fa-heart text-2xl text-amber-600" aria-hidden="true"></i>
                </div>
                <h2 class="text-2xl font-bold text-gray-800 mb-2">Dog Lap Day Calculator</h2>
                <p class="text-gray-600">Discover how often your dog deserves special lap time based on their unique traits</p>
            </div>

            <!-- Input Form -->
            <div class="max-w-4xl mx-auto space-y-6">
                <!-- Dog Basic Info -->
                <div class="bg-amber-50 p-6 rounded-lg">
                    <h3 class="text-lg font-semibold text-amber-800 mb-4 flex items-center">
                        <i class="fas fa-id-card mr-2" aria-hidden="true"></i>
                        Your Dog's Profile
                    </h3>
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="dogName" class="block text-sm font-medium text-gray-700 mb-2">Dog's Name</label>
                            <input type="text" id="dogName" placeholder="Max, Bella, Charlie..." class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent">
                            <div class="text-xs text-gray-500 mt-1">What's your furry friend's name?</div>
                        </div>

                        <div>
                            <label for="dogAge" class="block text-sm font-medium text-gray-700 mb-2">Age (years)</label>
                            <input type="number" id="dogAge" min="0" max="25" placeholder="3" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent">
                            <div class="text-xs text-gray-500 mt-1">How old is your pup?</div>
                        </div>
                    </div>
                </div>

                <!-- Dog Size & Breed -->
                <div class="grid md:grid-cols-2 gap-6">
                    <div>
                        <label for="dogSize" class="block text-sm font-medium text-gray-700 mb-2">Dog Size</label>
                        <select id="dogSize" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent">
                            <option value="">Select size...</option>
                            <option value="tiny">Tiny (under 10 lbs)</option>
                            <option value="small">Small (10-25 lbs)</option>
                            <option value="medium">Medium (25-60 lbs)</option>
                            <option value="large">Large (60-100 lbs)</option>
                            <option value="giant">Giant (over 100 lbs)</option>
                        </select>
                    </div>

                    <div>
                        <label for="breedType" class="block text-sm font-medium text-gray-700 mb-2">Breed Type</label>
                        <select id="breedType" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent">
                            <option value="">Select breed type...</option>
                            <option value="lap">Lap Dog (Chihuahua, Pug, etc.)</option>
                            <option value="companion">Companion (Golden Retriever, Beagle)</option>
                            <option value="working">Working (German Shepherd, Husky)</option>
                            <option value="sporting">Sporting (Labrador, Pointer)</option>
                            <option value="terrier">Terrier (Jack Russell, Bull Terrier)</option>
                            <option value="hound">Hound (Bloodhound, Greyhound)</option>
                            <option value="mixed">Mixed/Other</option>
                        </select>
                    </div>
                </div>

                <!-- Personality Traits -->
                <div class="bg-blue-50 p-6 rounded-lg">
                    <h3 class="text-lg font-semibold text-blue-800 mb-4 flex items-center">
                        <i class="fas fa-smile mr-2" aria-hidden="true"></i>
                        Personality Assessment
                    </h3>
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="energyLevel" class="block text-sm font-medium text-gray-700 mb-2">Energy Level</label>
                            <select id="energyLevel" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                <option value="">Select energy level...</option>
                                <option value="low">Low - Couch Potato</option>
                                <option value="moderate">Moderate - Balanced</option>
                                <option value="high">High - Always Ready to Play</option>
                                <option value="extreme">Extreme - Never Stops Moving</option>
                            </select>
                        </div>

                        <div>
                            <label for="cuddliness" class="block text-sm font-medium text-gray-700 mb-2">Cuddliness Factor</label>
                            <select id="cuddliness" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                <option value="">How cuddly?</option>
                                <option value="independent">Independent - Prefers Personal Space</option>
                                <option value="selective">Selective - Cuddles on Their Terms</option>
                                <option value="affectionate">Affectionate - Loves Cuddles</option>
                                <option value="velcro">Velcro Dog - Always Touching You</option>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Owner Preferences -->
                <div class="grid md:grid-cols-2 gap-6">
                    <div>
                        <label for="ownerSchedule" class="block text-sm font-medium text-gray-700 mb-2">Your Schedule</label>
                        <select id="ownerSchedule" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent">
                            <option value="">Your availability...</option>
                            <option value="very-busy">Very Busy - Limited Time</option>
                            <option value="busy">Busy - Some Free Time</option>
                            <option value="moderate">Moderate - Good Work-Life Balance</option>
                            <option value="flexible">Flexible - Lots of Free Time</option>
                        </select>
                    </div>

                    <div>
                        <label for="lapPreference" class="block text-sm font-medium text-gray-700 mb-2">Your Lap Preference</label>
                        <select id="lapPreference" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent">
                            <option value="">How do you feel about lap time?</option>
                            <option value="love">Love It - Bring on the Cuddles!</option>
                            <option value="enjoy">Enjoy It - Nice Bonding Time</option>
                            <option value="tolerate">Tolerate - Okay Sometimes</option>
                            <option value="limited">Prefer Limited Lap Time</option>
                        </select>
                    </div>
                </div>

                <!-- Special Factors -->
                <div class="bg-green-50 p-6 rounded-lg">
                    <h4 class="font-medium text-green-800 mb-3 flex items-center">
                        <i class="fas fa-star mr-2" aria-hidden="true"></i>
                        Special Considerations
                    </h4>
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
                        <label class="flex items-center">
                            <input type="checkbox" id="puppyStage" class="mr-2 text-green-600 focus:ring-green-500">
                            <span class="text-sm">Puppy Stage</span>
                        </label>
                        <label class="flex items-center">
                            <input type="checkbox" id="seniorDog" class="mr-2 text-green-600 focus:ring-green-500">
                            <span class="text-sm">Senior Dog</span>
                        </label>
                        <label class="flex items-center">
                            <input type="checkbox" id="anxiousDog" class="mr-2 text-green-600 focus:ring-green-500">
                            <span class="text-sm">Anxious/Stressed</span>
                        </label>
                        <label class="flex items-center">
                            <input type="checkbox" id="newAdoption" class="mr-2 text-green-600 focus:ring-green-500">
                            <span class="text-sm">Recently Adopted</span>
                        </label>
                    </div>
                </div>

                <!-- Calculate Button -->
                <div class="text-center">
                    <button id="calculateBtn" class="bg-amber-600 hover:bg-amber-700 text-white font-bold py-4 px-8 rounded-lg shadow-lg transform transition hover:scale-105">
                        <i class="fas fa-paw mr-2" aria-hidden="true"></i>
                        Calculate Lap Day Schedule
                    </button>
                </div>
            </div>

            <!-- Results Display -->
            <div id="resultsSection" class="hidden fade-in mt-8">
                <div class="bg-gradient-to-r from-amber-50 to-orange-50 border-2 border-amber-200 rounded-xl p-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-6 text-center">
                        <i class="fas fa-calendar-heart mr-2 text-amber-600" aria-hidden="true"></i>
                        <span id="dogNameDisplay">Your Dog's</span> Lap Day Schedule
                    </h3>
                    
                    <!-- Main Results -->
                    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-4 mb-6">
                        <div class="dog-card bg-white p-6 rounded-lg shadow text-center">
                            <div class="text-3xl font-bold text-amber-600 mb-2" id="lapDaysPerWeek">3</div>
                            <div class="text-sm text-gray-600">Lap Days Per Week</div>
                        </div>
                        <div class="dog-card bg-white p-6 rounded-lg shadow text-center">
                            <div class="text-2xl font-bold text-blue-600 mb-2" id="sessionDuration">30 min</div>
                            <div class="text-sm text-gray-600">Per Session</div>
                        </div>
                        <div class="dog-card bg-white p-6 rounded-lg shadow text-center">
                            <div class="text-2xl font-bold text-green-600 mb-2" id="bestTime">Evening</div>
                            <div class="text-sm text-gray-600">Best Time</div>
                        </div>
                    </div>

                    <!-- Personality Match -->
                    <div class="grid md:grid-cols-2 gap-6 mb-6">
                        <div class="bg-white p-6 rounded-lg shadow">
                            <h4 class="font-semibold text-gray-800 mb-4 flex items-center">
                                <i class="fas fa-heart mr-2 text-red-500" aria-hidden="true"></i>
                                Perfect Match Analysis
                            </h4>
                            <div id="matchAnalysis" class="space-y-3">
                                <!-- Analysis will be populated by JS -->
                            </div>
                        </div>

                        <div class="bg-white p-6 rounded-lg shadow">
                            <h4 class="font-semibold text-gray-800 mb-4 flex items-center">
                                <i class="fas fa-tips mr-2 text-blue-500" aria-hidden="true"></i>
                                Lap Time Tips
                            </h4>
                            <div id="lapTips" class="space-y-3">
                                <!-- Tips will be populated by JS -->
                            </div>
                        </div>
                    </div>

                    <!-- Weekly Schedule -->
                    <div class="bg-white p-6 rounded-lg shadow mb-6">
                        <h4 class="font-semibold text-gray-800 mb-4 flex items-center">
                            <i class="fas fa-calendar-week mr-2 text-purple-500" aria-hidden="true"></i>
                            Suggested Weekly Schedule
                        </h4>
                        <div id="weeklySchedule" class="grid grid-cols-7 gap-2">
                            <!-- Schedule will be populated by JS -->
                        </div>
                    </div>

                    <!-- Fun Stats -->
                    <div class="bg-gradient-to-r from-purple-100 to-pink-100 p-6 rounded-lg mb-6">
                        <h4 class="font-semibold text-gray-800 mb-4 flex items-center">
                            <i class="fas fa-chart-bar mr-2 text-purple-600" aria-hidden="true"></i>
                            Fun Lap Statistics
                        </h4>
                        <div id="funStats" class="grid md:grid-cols-3 gap-4 text-center">
                            <!-- Fun stats will be populated by JS -->
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="flex flex-wrap gap-3 justify-center">
                        <button id="shareBtn" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg transition-colors">
                            <i class="fas fa-share mr-2" aria-hidden="true"></i>
                            Share Results
                        </button>
                        <button id="saveBtn" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition-colors">
                            <i class="fas fa-bookmark mr-2" aria-hidden="true"></i>
                            Save Schedule
                        </button>
                        <button id="resetBtn" class="bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded-lg transition-colors">
                            <i class="fas fa-refresh mr-2" aria-hidden="true"></i>
                            Calculate for Another Dog
                        </button>
                    </div>
                </div>
            </div>
        </section>

        <!-- Information Sections -->
        <div class="grid md:grid-cols-2 gap-8">
            <!-- Dog Size Guide -->
            <section class="bg-white rounded-xl shadow-lg p-8">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                    <i class="fas fa-ruler mr-3 text-green-500" aria-hidden="true"></i>
                    Dog Size Guide
                </h2>
                
                <div class="space-y-6">
                    <div class="dog-card dog-size-small p-4 rounded-lg">
                        <h3 class="text-lg font-semibold text-green-800 mb-2">Small Dogs (10-25 lbs)</h3>
                        <p class="text-green-700 text-sm mb-2">Perfect lap companions! Usually love cuddling and seek warmth.</p>
                        <p class="text-xs text-green-600">Examples: Pug, Beagle, French Bulldog</p>
                    </div>
                    
                    <div class="dog-card dog-size-medium p-4 rounded-lg">
                        <h3 class="text-lg font-semibold text-blue-800 mb-2">Medium Dogs (25-60 lbs)</h3>
                        <p class="text-blue-700 text-sm mb-2">Great balance of lap time and independence. Enjoy moderate cuddling.</p>
                        <p class="text-xs text-blue-600">Examples: Golden Retriever, Border Collie</p>
                    </div>
                    
                    <div class="dog-card dog-size-large p-4 rounded-lg">
                        <h3 class="text-lg font-semibold text-purple-800 mb-2">Large Dogs (60-100 lbs)</h3>
                        <p class="text-purple-700 text-sm mb-2">Gentle giants who think they're lap dogs! May need floor cuddles.</p>
                        <p class="text-xs text-purple-600">Examples: German Shepherd, Labrador</p>
                    </div>
                </div>
            </section>

            <!-- Benefits of Lap Time -->
            <section class="bg-white rounded-xl shadow-lg p-8">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                    <i class="fas fa-heart mr-3 text-red-500" aria-hidden="true"></i>
                    Benefits of Lap Time
                </h2>
                
                <div class="space-y-4">
                    <div class="flex items-start space-x-3">
                        <i class="fas fa-shield-heart text-blue-500 mt-1" aria-hidden="true"></i>
                        <div>
                            <h3 class="font-semibold text-gray-800">Bonding & Trust</h3>
                            <p class="text-gray-600 text-sm">Strengthens the human-dog bond and builds mutual trust.</p>
                        </div>
                    </div>
                    
                    <div class="flex items-start space-x-3">
                        <i class="fas fa-leaf text-green-500 mt-1" aria-hidden="true"></i>
                        <div>
                            <h3 class="font-semibold text-gray-800">Stress Relief</h3>
                            <p class="text-gray-600 text-sm">Reduces stress and anxiety for both you and your dog.</p>
                        </div>
                    </div>
                    
                    <div class="flex items-start space-x-3">
                        <i class="fas fa-thermometer-half text-orange-500 mt-1" aria-hidden="true"></i>
                        <div>
                            <h3 class="font-semibold text-gray-800">Temperature Regulation</h3>
                            <p class="text-gray-600 text-sm">Helps smaller dogs maintain body temperature in cold weather.</p>
                        </div>
                    </div>
                    
                    <div class="flex items-start space-x-3">
                        <i class="fas fa-brain text-purple-500 mt-1" aria-hidden="true"></i>
                        <div>
                            <h3 class="font-semibold text-gray-800">Mental Health</h3>
                            <p class="text-gray-600 text-sm">Boosts oxytocin production and improves overall well-being.</p>
                        </div>
                    </div>
                    
                    <div class="flex items-start space-x-3">
                        <i class="fas fa-bed text-indigo-500 mt-1" aria-hidden="true"></i>
                        <div>
                            <h3 class="font-semibold text-gray-800">Better Sleep</h3>
                            <p class="text-gray-600 text-sm">Regular lap time can improve sleep quality for both parties.</p>
                        </div>
                    </div>
                </div>
            </section>
        </div>

        <!-- FAQs Section -->
        <section class="bg-white rounded-xl shadow-lg p-8 mt-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                <i class="fas fa-question-circle mr-3 text-purple-600" aria-hidden="true"></i>
                Frequently Asked Questions
            </h2>
            
            <div class="space-y-6">
                <div class="border-l-4 border-blue-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">Is this based on real science?</h3>
                    <p class="text-gray-600">While fun and entertaining, this calculator is based on general dog behavior patterns and breed characteristics for educational and entertainment purposes.</p>
                </div>
                
                <div class="border-l-4 border-green-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">What if my dog doesn't like lap time?</h3>
                    <p class="text-gray-600">Every dog is unique! Some prefer being near you rather than on you. Respect your dog's personal space preferences and find other bonding activities.</p>
                </div>
                
                <div class="border-l-4 border-orange-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">Can large dogs have lap time too?</h3>
                    <p class="text-gray-600">Absolutely! Many large dogs think they're lap dogs. You might need to modify with floor cuddles or sharing the couch instead.</p>
                </div>
                
                <div class="border-l-4 border-purple-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">When is National Dog Day?</h3>
                    <p class="text-gray-600">National Dog Day is celebrated on August 26th each year, celebrating the bond between humans and dogs while encouraging adoption from shelters.</p>
                </div>
            </div>
        </section>
    </main>

    <?php include 'footer.php'; ?>

    <!-- JavaScript -->
    <script>
        class DogLapDayCalculator {
            constructor() {
                this.dogProfile = {};
                this.lapSchedule = {};
                this.init();
            }

            init() {
                this.bindEvents();
            }

            bindEvents() {
                document.getElementById('calculateBtn').addEventListener('click', () => this.calculateLapSchedule());
                document.getElementById('shareBtn')?.addEventListener('click', () => this.shareResults());
                document.getElementById('saveBtn')?.addEventListener('click', () => this.saveSchedule());
                document.getElementById('resetBtn')?.addEventListener('click', () => this.resetCalculator());
            }

            calculateLapSchedule() {
                if (!this.collectDogProfile()) {
                    return;
                }

                this.lapSchedule = this.generateLapSchedule();
                this.displayResults();
            }

            collectDogProfile() {
                const name = document.getElementById('dogName').value.trim();
                const age = parseInt(document.getElementById('dogAge').value);
                const size = document.getElementById('dogSize').value;
                const breedType = document.getElementById('breedType').value;
                const energyLevel = document.getElementById('energyLevel').value;
                const cuddliness = document.getElementById('cuddliness').value;
                const ownerSchedule = document.getElementById('ownerSchedule').value;
                const lapPreference = document.getElementById('lapPreference').value;

                if (!size || !breedType || !energyLevel || !cuddliness || !ownerSchedule || !lapPreference) {
                    alert('Please fill in all the required fields to calculate your dog\'s lap schedule!');
                    return false;
                }

                this.dogProfile = {
                    name: name || 'Your Dog',
                    age: age || 3,
                    size,
                    breedType,
                    energyLevel,
                    cuddliness,
                    ownerSchedule,
                    lapPreference,
                    puppyStage: document.getElementById('puppyStage').checked,
                    seniorDog: document.getElementById('seniorDog').checked,
                    anxiousDog: document.getElementById('anxiousDog').checked,
                    newAdoption: document.getElementById('newAdoption').checked
                };

                return true;
            }

            generateLapSchedule() {
                let lapDaysPerWeek = this.calculateLapDays();
                let sessionDuration = this.calculateSessionDuration();
                let bestTime = this.calculateBestTime();
                
                // Apply special modifiers
                if (this.dogProfile.puppyStage) lapDaysPerWeek = Math.min(7, lapDaysPerWeek + 1);
                if (this.dogProfile.seniorDog) sessionDuration += 15;
                if (this.dogProfile.anxiousDog) lapDaysPerWeek = Math.min(7, lapDaysPerWeek + 2);
                if (this.dogProfile.newAdoption) lapDaysPerWeek = Math.min(7, lapDaysPerWeek + 1);

                return {
                    lapDaysPerWeek: Math.max(1, Math.min(7, lapDaysPerWeek)),
                    sessionDuration: Math.max(10, Math.min(120, sessionDuration)),
                    bestTime,
                    weeklySchedule: this.generateWeeklySchedule(lapDaysPerWeek),
                    compatibility: this.calculateCompatibility(),
                    tips: this.generateTips(),
                    funStats: this.generateFunStats(lapDaysPerWeek, sessionDuration)
                };
            }

            calculateLapDays() {
                let baseDays = 3;

                // Size factor
                const sizeFactors = {
                    tiny: 5, small: 4, medium: 3, large: 2, giant: 1
                };
                baseDays = sizeFactors[this.dogProfile.size] || 3;

                // Breed factor
                const breedFactors = {
                    lap: 2, companion: 1, working: -1, sporting: 0, terrier: 0, hound: -1, mixed: 0
                };
                baseDays += breedFactors[this.dogProfile.breedType] || 0;

                // Cuddliness factor
                const cuddlinessFactors = {
                    independent: -2, selective: -1, affectionate: 1, velcro: 2
                };
                baseDays += cuddlinessFactors[this.dogProfile.cuddliness] || 0;

                // Owner preference factor
                const preferenceFactors = {
                    love: 2, enjoy: 1, tolerate: 0, limited: -2
                };
                baseDays += preferenceFactors[this.dogProfile.lapPreference] || 0;

                // Owner schedule factor
                const scheduleFactors = {
                    'very-busy': -1, busy: 0, moderate: 1, flexible: 2
                };
                baseDays += scheduleFactors[this.dogProfile.ownerSchedule] || 0;

                return Math.max(1, Math.min(7, baseDays));
            }

            calculateSessionDuration() {
                let baseDuration = 30;

                // Size factor - smaller dogs can handle longer sessions
                const sizeDurations = {
                    tiny: 45, small: 40, medium: 30, large: 25, giant: 20
                };
                baseDuration = sizeDurations[this.dogProfile.size] || 30;

                // Energy level factor
                const energyFactors = {
                    low: 20, moderate: 0, high: -10, extreme: -15
                };
                baseDuration += energyFactors[this.dogProfile.energyLevel] || 0;

                // Age factor
                if (this.dogProfile.age < 1) baseDuration += 10; // Puppies need more comfort
                if (this.dogProfile.age > 8) baseDuration += 15; // Seniors appreciate longer cuddles

                return Math.max(10, Math.min(120, baseDuration));
            }

            calculateBestTime() {
                const energyLevel = this.dogProfile.energyLevel;
                const ownerSchedule = this.dogProfile.ownerSchedule;

                if (energyLevel === 'low' || this.dogProfile.seniorDog) return 'Morning';
                if (energyLevel === 'extreme') return 'After Exercise';
                if (ownerSchedule === 'very-busy' || ownerSchedule === 'busy') return 'Evening';
                
                return 'Evening';
            }

            generateWeeklySchedule(lapDays) {
                const days = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
                const schedule = [];

                // Distribute lap days evenly throughout the week
                const interval = Math.floor(7 / lapDays);
                let nextLapDay = 0;

                for (let i = 0; i < 7; i++) {
                    const isLapDay = i === nextLapDay && lapDays > 0;
                    schedule.push({
                        day: days[i],
                        isLapDay: isLapDay,
                        activity: isLapDay ? 'Lap Time! 🐕' : 'Regular Play'
                    });

                    if (isLapDay) {
                        lapDays--;
                        nextLapDay = Math.min(6, nextLapDay + interval + 1);
                    }
                }

                return schedule;
            }

            calculateCompatibility() {
                let score = 50; // Base compatibility

                // Size compatibility
                if (this.dogProfile.size === 'tiny' || this.dogProfile.size === 'small') score += 20;
                if (this.dogProfile.size === 'giant') score -= 10;

                // Cuddliness compatibility
                const cuddlinessScores = {
                    independent: -20, selective: -5, affectionate: 15, velcro: 25
                };
                score += cuddlinessScores[this.dogProfile.cuddliness] || 0;

                // Owner preference compatibility
                const preferenceScores = {
                    love: 20, enjoy: 10, tolerate: -5, limited: -15
                };
                score += preferenceScores[this.dogProfile.lapPreference] || 0;

                // Breed compatibility
                if (this.dogProfile.breedType === 'lap') score += 15;
                if (this.dogProfile.breedType === 'companion') score += 10;

                return Math.max(0, Math.min(100, score));
            }

            generateTips() {
                const tips = [];
                
                if (this.dogProfile.size === 'large' || this.dogProfile.size === 'giant') {
                    tips.push({
                        icon: 'fas fa-couch',
                        text: 'Consider floor cuddles or sharing the couch instead of traditional lap sitting.'
                    });
                }

                if (this.dogProfile.energyLevel === 'high' || this.dogProfile.energyLevel === 'extreme') {
                    tips.push({
                        icon: 'fas fa-running',
                        text: 'Schedule lap time after exercise when your dog is more likely to relax.'
                    });
                }

                if (this.dogProfile.cuddliness === 'independent') {
                    tips.push({
                        icon: 'fas fa-user-friends',
                        text: 'Respect personal space - try sitting nearby rather than forcing cuddles.'
                    });
                }

                if (this.dogProfile.anxiousDog) {
                    tips.push({
                        icon: 'fas fa-heart-broken',
                        text: 'Extra lap time can help reduce anxiety and build confidence.'
                    });
                }

                if (this.dogProfile.puppyStage) {
                    tips.push({
                        icon: 'fas fa-baby',
                        text: 'Puppies need lots of comfort and bonding time for proper development.'
                    });
                }

                // Add default tips if none specific
                if (tips.length === 0) {
                    tips.push(
                        {
                            icon: 'fas fa-clock',
                            text: 'Start with shorter sessions and gradually increase based on comfort.'
                        },
                        {
                            icon: 'fas fa-thermometer-half',
                            text: 'Lap time is especially important during cold weather for warmth.'
                        }
                    );
                }

                return tips;
            }

            generateFunStats(lapDays, duration) {
                const weeklyMinutes = lapDays * duration;
                const yearlyHours = Math.round((weeklyMinutes * 52) / 60);
                const yearlyTreats = lapDays * 52 * 2; // Assume 2 treats per lap session

                return [
                    {
                        value: weeklyMinutes,
                        label: 'Minutes Per Week',
                        icon: 'fas fa-clock',
                        color: 'text-blue-600'
                    },
                    {
                        value: yearlyHours,
                        label: 'Hours Per Year',
                        icon: 'fas fa-calendar-alt',
                        color: 'text-green-600'
                    },
                    {
                        value: yearlyTreats,
                        label: 'Treats Earned',
                        icon: 'fas fa-bone',
                        color: 'text-orange-600'
                    }
                ];
            }

            displayResults() {
                const resultsSection = document.getElementById('resultsSection');
                resultsSection.classList.remove('hidden');

                // Update dog name
                document.getElementById('dogNameDisplay').textContent = this.dogProfile.name + "'s";

                // Update main stats
                document.getElementById('lapDaysPerWeek').textContent = this.lapSchedule.lapDaysPerWeek;
                document.getElementById('sessionDuration').textContent = this.lapSchedule.sessionDuration + ' min';
                document.getElementById('bestTime').textContent = this.lapSchedule.bestTime;

                // Update compatibility analysis
                this.updateMatchAnalysis();
                this.updateLapTips();
                this.updateWeeklySchedule();
                this.updateFunStats();

                // Scroll to results
                resultsSection.scrollIntoView({ behavior: 'smooth' });
            }

            updateMatchAnalysis() {
                const compatibility = this.lapSchedule.compatibility;
                let grade, message, color;

                if (compatibility >= 80) {
                    grade = 'Perfect Match!';
                    message = 'You and your dog are made for lap time!';
                    color = 'text-green-600';
                } else if (compatibility >= 60) {
                    grade = 'Great Compatibility';
                    message = 'Lap time should work well for both of you.';
                    color = 'text-blue-600';
                } else if (compatibility >= 40) {
                    grade = 'Moderate Match';
                    message = 'Some adjustment may be needed for perfect lap sessions.';
                    color = 'text-yellow-600';
                } else {
                    grade = 'Needs Patience';
                    message = 'Take it slow and respect boundaries.';
                    color = 'text-red-600';
                }

                const analysisHtml = `
                    <div class="flex items-center justify-between mb-3">
                        <span class="text-lg font-bold ${color}">${grade}</span>
                        <span class="text-2xl font-bold ${color}">${compatibility}%</span>
                    </div>
                    <p class="text-gray-600 text-sm">${message}</p>
                    <div class="mt-3 bg-gray-200 rounded-full h-3">
                        <div class="bg-gradient-to-r from-red-500 via-yellow-500 to-green-500 h-3 rounded-full" style="width: ${compatibility}%"></div>
                    </div>
                `;

                document.getElementById('matchAnalysis').innerHTML = analysisHtml;
            }

            updateLapTips() {
                const tipsHtml = this.lapSchedule.tips.map(tip => `
                    <div class="flex items-start space-x-3">
                        <i class="${tip.icon} text-blue-500 mt-1" aria-hidden="true"></i>
                        <p class="text-sm text-gray-700">${tip.text}</p>
                    </div>
                `).join('');

                document.getElementById('lapTips').innerHTML = tipsHtml;
            }

            updateWeeklySchedule() {
                const scheduleHtml = this.lapSchedule.weeklySchedule.map(day => {
                    const bgColor = day.isLapDay ? 'bg-amber-100 border-amber-300' : 'bg-gray-50 border-gray-200';
                    const textColor = day.isLapDay ? 'text-amber-800' : 'text-gray-600';
                    
                    return `
                        <div class="p-3 rounded-lg border-2 ${bgColor} text-center">
                            <div class="font-semibold ${textColor} mb-1">${day.day}</div>
                            <div class="text-xs ${textColor}">${day.activity}</div>
                        </div>
                    `;
                }).join('');

                document.getElementById('weeklySchedule').innerHTML = scheduleHtml;
            }

            updateFunStats() {
                const statsHtml = this.lapSchedule.funStats.map(stat => `
                    <div class="bg-white p-4 rounded-lg shadow">
                        <i class="${stat.icon} ${stat.color} text-2xl mb-2" aria-hidden="true"></i>
                        <div class="text-2xl font-bold ${stat.color}">${stat.value}</div>
                        <div class="text-sm text-gray-600">${stat.label}</div>
                    </div>
                `).join('');

                document.getElementById('funStats').innerHTML = statsHtml;
            }

            shareResults() {
                const dogName = this.dogProfile.name;
                const lapDays = this.lapSchedule.lapDaysPerWeek;
                const duration = this.lapSchedule.sessionDuration;
                const compatibility = this.lapSchedule.compatibility;

                const shareText = `🐕 ${dogName} needs ${lapDays} lap days per week (${duration} min each)! Our compatibility: ${compatibility}%. Calculate your dog's lap schedule: ${window.location.href}`;

                if (navigator.share) {
                    navigator.share({
                        title: `${dogName}'s Lap Day Schedule`,
                        text: shareText
                    });
                } else {
                    navigator.clipboard.writeText(shareText).then(() => {
                        alert('Lap schedule copied to clipboard!');
                    });
                }
            }

            saveSchedule() {
                const scheduleData = {
                    dogProfile: this.dogProfile,
                    lapSchedule: this.lapSchedule,
                    generatedDate: new Date().toISOString()
                };
                
                const blob = new Blob([JSON.stringify(scheduleData, null, 2)], { type: 'application/json' });
                const url = URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.href = url;
                a.download = `${this.dogProfile.name.replace(/\s+/g, '_')}_lap_schedule.json`;
                a.click();
                URL.revokeObjectURL(url);
            }

            resetCalculator() {
                // Reset all form fields
                document.getElementById('dogName').value = '';
                document.getElementById('dogAge').value = '';
                document.getElementById('dogSize').value = '';
                document.getElementById('breedType').value = '';
                document.getElementById('energyLevel').value = '';
                document.getElementById('cuddliness').value = '';
                document.getElementById('ownerSchedule').value = '';
                document.getElementById('lapPreference').value = '';
                document.getElementById('puppyStage').checked = false;
                document.getElementById('seniorDog').checked = false;
                document.getElementById('anxiousDog').checked = false;
                document.getElementById('newAdoption').checked = false;
                
                // Hide results
                document.getElementById('resultsSection').classList.add('hidden');
                
                // Clear profile
                this.dogProfile = {};
                this.lapSchedule = {};
            }
        }

        // Initialize calculator when page loads
        document.addEventListener('DOMContentLoaded', () => {
            new DogLapDayCalculator();
        });

        // Add scroll animations
        document.addEventListener('DOMContentLoaded', () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('fade-in');
                    }
                });
            });

            document.querySelectorAll('section').forEach(el => {
                observer.observe(el);
            });
        });
    </script>
</body>
</html>