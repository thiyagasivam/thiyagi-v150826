<?php include 'header.php'; ?>

    <title>Text to Slug Converter 2026 - URL Slug Generator Tool | Thiyagi.com</title>
    <meta name="description" content="Convert text to URL-friendly slugs instantly. Free text to slug converter 2026 with multiple formatting options, special character handling, and SEO optimization features.">
    <meta name="keywords" content="text to slug converter 2026, URL slug generator, SEO slug tool, text formatter, URL friendly converter, web development tools, permalink generator">
    <meta name="author" content="Thiyagi">
        
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="Text to Slug Converter 2026 - URL Slug Generator Tool">
    <meta property="og:description" content="Convert text to URL-friendly slugs instantly. Free text to slug converter with multiple formatting options and SEO optimization features.">
    <meta property="og:url" content="https://www.thiyagi.com/text-to-slug">
    <meta property="og:type" content="website">
    <meta property="og:image" content="https://www.thiyagi.com/nt.png">
    
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Text to Slug Converter 2026 - URL Slug Generator Tool">
    <meta name="twitter:description" content="Convert text to URL-friendly slugs instantly. Free text to slug converter with multiple formatting options and SEO optimization features.">
    <meta name="twitter:image" content="https://www.thiyagi.com/nt.png">
    
    <!-- Canonical URL -->
    
    <!-- Tailwind CSS -->

<style>
    .gradient-bg {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    .result-card {
        transition: all 0.3s ease;
    }
    .result-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }
    .fade-in {
        animation: fadeIn 0.5s ease-in-out;
    }
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .code-box {
        background: #1e293b;
        border: 1px solid #334155;
    }
    .option-card {
        transition: all 0.3s ease;
        cursor: pointer;
    }
    .option-card:hover {
        background: #f8fafc;
        border-color: #6366f1;
    }
    .option-card.active {
        background: #eef2ff;
        border-color: #6366f1;
        box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
    }
</style>

<!-- JSON-LD Structured Data -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "Text to Slug Converter 2026",
  "description": "Convert any text to URL-friendly slugs with customizable formatting options, special character handling, and SEO optimization features for web developers and content creators.",
  "url": "https://www.thiyagi.com/text-to-slug",
  "applicationCategory": "DeveloperApplication",
  "operatingSystem": "Any",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "USD"
  },
  "creator": {
    "@type": "Organization",
    "name": "Thiyagi.com"
  },
  "breadcrumb": {
    "@type": "BreadcrumbList",
    "itemListElement": [
      {
        "@type": "ListItem",
        "position": 1,
        "name": "Home",
        "item": "https://www.thiyagi.com"
      },
      {
        "@type": "ListItem",
        "position": 2,
        "name": "Text to Slug Converter",
        "item": "https://www.thiyagi.com/text-to-slug"
      }
    ]
  }
}
</script>

<!-- FAQ Structured Data -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "What is a URL slug?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "A URL slug is the part of a URL that identifies a particular page on a website in an easy-to-read form. It typically contains only lowercase letters, numbers, and hyphens, making it SEO-friendly and user-readable."
      }
    },
    {
      "@type": "Question",
      "name": "How does a text to slug converter work?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "A text to slug converter transforms regular text into URL-friendly format by converting to lowercase, replacing spaces with hyphens, removing special characters, and ensuring the result follows URL best practices for SEO optimization."
      }
    },
    {
      "@type": "Question",
      "name": "Why are URL slugs important for SEO?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "URL slugs are important for SEO because they help search engines understand page content, improve user experience with readable URLs, and can include relevant keywords that boost search rankings."
      }
    },
    {
      "@type": "Question",
      "name": "What characters are allowed in URL slugs?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "URL slugs should typically contain only lowercase letters (a-z), numbers (0-9), and hyphens (-). Avoid spaces, special characters, and uppercase letters for maximum compatibility and SEO benefits."
      }
    },
    {
      "@type": "Question",
      "name": "Can I customize slug generation options?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Yes, our text to slug converter offers various customization options including separator choice (hyphens or underscores), case conversion, character limits, and special character handling preferences."
      }
    }
  ]
}
</script>

<body class="bg-gray-50">
    <!-- Header -->
    <header class="gradient-bg shadow-lg">
        <div class="max-w-6xl mx-auto px-4 py-6">
            <div class="flex items-center justify-center">
                <div class="flex items-center space-x-4">
                    <div class="bg-white p-3 rounded-full shadow-lg">
                        <i class="fas fa-link text-2xl text-purple-600" aria-hidden="true"></i>
                    </div>
                    <div>
                        <h1 class="text-3xl font-bold text-white">Text to Slug Converter</h1>
                        <p class="text-purple-100">Convert text to URL-friendly slugs instantly</p>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Breadcrumb -->
    <nav class="bg-white border-b" aria-label="Breadcrumb">
        <div class="max-w-6xl mx-auto px-4 py-3">
            <ol class="flex items-center space-x-2 text-sm">
                <li><a href="/" class="text-gray-500 hover:text-gray-700">Home</a></li>
                <li><i class="fas fa-chevron-right text-gray-400" aria-hidden="true"></i></li>
                <li class="text-gray-900 font-medium">Text to Slug Converter</li>
            </ol>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-6xl mx-auto px-4 py-8">
        <!-- Converter Tool -->
        <section class="bg-white rounded-xl shadow-lg p-8 mb-8">
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-purple-100 rounded-full mb-4">
                    <i class="fas fa-magic text-2xl text-purple-600" aria-hidden="true"></i>
                </div>
                <h2 class="text-2xl font-bold text-gray-800 mb-2">URL Slug Generator</h2>
                <p class="text-gray-600">Transform any text into SEO-friendly URL slugs with customizable options</p>
            </div>

            <!-- Input Section -->
            <div class="mb-8">
                <label for="inputText" class="block text-sm font-medium text-gray-700 mb-2">
                    <i class="fas fa-edit mr-2 text-purple-600" aria-hidden="true"></i>
                    Enter your text to convert
                </label>
                <textarea 
                    id="inputText" 
                    rows="4" 
                    placeholder="Enter your text here... (e.g., How to Create Amazing Blog Posts in 2026)"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent resize-vertical"
                ></textarea>
                <div class="flex justify-between items-center mt-2">
                    <span id="charCount" class="text-sm text-gray-500">0 characters</span>
                    <button id="clearBtn" class="text-sm text-gray-500 hover:text-red-500 transition-colors">
                        <i class="fas fa-times mr-1" aria-hidden="true"></i>
                        Clear
                    </button>
                </div>
            </div>

            <!-- Options Section -->
            <div class="grid md:grid-cols-2 gap-8 mb-8">
                <!-- Formatting Options -->
                <div class="space-y-4">
                    <h3 class="text-lg font-semibold text-gray-800 flex items-center">
                        <i class="fas fa-cogs mr-2 text-purple-600" aria-hidden="true"></i>
                        Formatting Options
                    </h3>
                    
                    <div class="space-y-3">
                        <div class="option-card border-2 border-gray-200 rounded-lg p-3 active" data-option="separator">
                            <label class="flex items-center cursor-pointer">
                                <input type="radio" name="separator" value="hyphen" checked class="mr-3 text-purple-600">
                                <div>
                                    <div class="font-medium text-gray-800">Use Hyphens (-)</div>
                                    <div class="text-sm text-gray-600">Recommended for SEO</div>
                                </div>
                            </label>
                        </div>

                        <div class="option-card border-2 border-gray-200 rounded-lg p-3" data-option="separator">
                            <label class="flex items-center cursor-pointer">
                                <input type="radio" name="separator" value="underscore" class="mr-3 text-purple-600">
                                <div>
                                    <div class="font-medium text-gray-800">Use Underscores (_)</div>
                                    <div class="text-sm text-gray-600">Alternative separator</div>
                                </div>
                            </label>
                        </div>

                        <div class="option-card border-2 border-gray-200 rounded-lg p-3" data-option="case">
                            <label class="flex items-center cursor-pointer">
                                <input type="checkbox" id="preserveCase" class="mr-3 text-purple-600">
                                <div>
                                    <div class="font-medium text-gray-800">Preserve Original Case</div>
                                    <div class="text-sm text-gray-600">Keep uppercase letters</div>
                                </div>
                            </label>
                        </div>
                    </div>
                </div>

                <!-- Advanced Options -->
                <div class="space-y-4">
                    <h3 class="text-lg font-semibold text-gray-800 flex items-center">
                        <i class="fas fa-sliders-h mr-2 text-purple-600" aria-hidden="true"></i>
                        Advanced Options
                    </h3>
                    
                    <div class="space-y-3">
                        <div>
                            <label for="maxLength" class="block text-sm font-medium text-gray-700 mb-2">Maximum Length</label>
                            <select id="maxLength" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500">
                                <option value="0">No limit</option>
                                <option value="30">30 characters</option>
                                <option value="50" selected>50 characters</option>
                                <option value="75">75 characters</option>
                                <option value="100">100 characters</option>
                            </select>
                        </div>

                        <div class="option-card border-2 border-gray-200 rounded-lg p-3" data-option="special">
                            <label class="flex items-center cursor-pointer">
                                <input type="checkbox" id="removeNumbers" class="mr-3 text-purple-600">
                                <div>
                                    <div class="font-medium text-gray-800">Remove Numbers</div>
                                    <div class="text-sm text-gray-600">Strip all numeric characters</div>
                                </div>
                            </label>
                        </div>

                        <div class="option-card border-2 border-gray-200 rounded-lg p-3" data-option="special">
                            <label class="flex items-center cursor-pointer">
                                <input type="checkbox" id="strictMode" class="mr-3 text-purple-600">
                                <div>
                                    <div class="font-medium text-gray-800">Strict Mode</div>
                                    <div class="text-sm text-gray-600">Extra character filtering</div>
                                </div>
                            </label>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Convert Button -->
            <div class="text-center mb-8">
                <button id="convertBtn" class="bg-purple-600 hover:bg-purple-700 text-white font-bold py-4 px-8 rounded-lg shadow-lg transform transition hover:scale-105">
                    <i class="fas fa-magic mr-2" aria-hidden="true"></i>
                    Convert to Slug
                </button>
            </div>

            <!-- Results Display -->
            <div id="resultsSection" class="hidden fade-in">
                <div class="bg-gradient-to-r from-purple-50 to-blue-50 border-2 border-purple-200 rounded-xl p-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-6 text-center">
                        <i class="fas fa-check-circle mr-2 text-green-500" aria-hidden="true"></i>
                        Generated URL Slug
                    </h3>
                    
                    <!-- Slug Result -->
                    <div class="result-card bg-white p-6 rounded-lg shadow mb-6">
                        <div class="flex items-center justify-between mb-3">
                            <h4 class="font-semibold text-gray-800 flex items-center">
                                <i class="fas fa-link mr-2 text-purple-600" aria-hidden="true"></i>
                                Your URL Slug
                            </h4>
                            <button id="copyBtn" class="text-purple-600 hover:text-purple-700 transition-colors">
                                <i class="fas fa-copy mr-1" aria-hidden="true"></i>
                                Copy
                            </button>
                        </div>
                        <div class="code-box p-4 rounded-lg text-white font-mono text-lg">
                            <span id="slugResult">your-generated-slug-will-appear-here</span>
                        </div>
                        <div class="mt-3 flex items-center justify-between text-sm text-gray-600">
                            <span>Length: <span id="slugLength">0</span> characters</span>
                            <span id="copyStatus" class="text-green-600 hidden">
                                <i class="fas fa-check mr-1" aria-hidden="true"></i>
                                Copied!
                            </span>
                        </div>
                    </div>

                    <!-- Preview Examples -->
                    <div class="bg-white p-6 rounded-lg shadow">
                        <h4 class="font-semibold text-gray-800 mb-4 flex items-center">
                            <i class="fas fa-eye mr-2 text-blue-500" aria-hidden="true"></i>
                            URL Preview Examples
                        </h4>
                        <div class="space-y-3">
                            <div class="p-3 bg-gray-50 rounded-lg">
                                <div class="text-sm text-gray-600 mb-1">WordPress Permalink:</div>
                                <div class="font-mono text-sm text-blue-600">https://example.com/<span id="wpPreview">your-slug</span>/</div>
                            </div>
                            <div class="p-3 bg-gray-50 rounded-lg">
                                <div class="text-sm text-gray-600 mb-1">Blog Post URL:</div>
                                <div class="font-mono text-sm text-blue-600">https://blog.example.com/posts/<span id="blogPreview">your-slug</span></div>
                            </div>
                            <div class="p-3 bg-gray-50 rounded-lg">
                                <div class="text-sm text-gray-600 mb-1">Product Page:</div>
                                <div class="font-mono text-sm text-blue-600">https://shop.example.com/products/<span id="productPreview">your-slug</span></div>
                            </div>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="mt-6 flex flex-wrap gap-3 justify-center">
                        <button id="newSlugBtn" class="bg-purple-600 hover:bg-purple-700 text-white px-4 py-2 rounded-lg transition-colors">
                            <i class="fas fa-plus mr-2" aria-hidden="true"></i>
                            Create New Slug
                        </button>
                        <button id="downloadBtn" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg transition-colors">
                            <i class="fas fa-download mr-2" aria-hidden="true"></i>
                            Download List
                        </button>
                        <button id="shareBtn" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition-colors">
                            <i class="fas fa-share mr-2" aria-hidden="true"></i>
                            Share Tool
                        </button>
                    </div>
                </div>
            </div>
        </section>

        <!-- Quick Examples -->
        <section class="bg-white rounded-xl shadow-lg p-8 mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                <i class="fas fa-lightbulb mr-3 text-yellow-500" aria-hidden="true"></i>
                Quick Examples
            </h2>
            
            <div class="grid md:grid-cols-3 gap-6">
                <div class="example-card bg-gray-50 p-4 rounded-lg cursor-pointer hover:bg-gray-100 transition-colors" data-example="How to Create Amazing Blog Posts in 2026">
                    <h3 class="font-semibold text-gray-800 mb-2">Blog Title</h3>
                    <div class="text-sm text-gray-600 mb-2">Input: "How to Create Amazing Blog Posts in 2026"</div>
                    <div class="text-sm text-purple-600 font-mono">Output: how-to-create-amazing-blog-posts-in-2026</div>
                </div>

                <div class="example-card bg-gray-50 p-4 rounded-lg cursor-pointer hover:bg-gray-100 transition-colors" data-example="Best SEO Practices & Tips for Beginners!">
                    <h3 class="font-semibold text-gray-800 mb-2">SEO Article</h3>
                    <div class="text-sm text-gray-600 mb-2">Input: "Best SEO Practices & Tips for Beginners!"</div>
                    <div class="text-sm text-purple-600 font-mono">Output: best-seo-practices-tips-for-beginners</div>
                </div>

                <div class="example-card bg-gray-50 p-4 rounded-lg cursor-pointer hover:bg-gray-100 transition-colors" data-example="JavaScript ES6: Modern Web Development">
                    <h3 class="font-semibold text-gray-800 mb-2">Tech Tutorial</h3>
                    <div class="text-sm text-gray-600 mb-2">Input: "JavaScript ES6: Modern Web Development"</div>
                    <div class="text-sm text-purple-600 font-mono">Output: javascript-es6-modern-web-development</div>
                </div>
            </div>

            <div class="mt-6 text-center">
                <button id="tryExamplesBtn" class="text-purple-600 hover:text-purple-700 text-sm font-medium">
                    <i class="fas fa-play mr-1" aria-hidden="true"></i>
                    Click any example to try it
                </button>
            </div>
        </section>

        <!-- Features & Benefits -->
        <section class="bg-white rounded-xl shadow-lg p-8 mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                <i class="fas fa-star mr-3 text-yellow-500" aria-hidden="true"></i>
                Features & Benefits
            </h2>
            
            <div class="grid md:grid-cols-3 gap-6">
                <div class="text-center p-6 bg-blue-50 rounded-lg">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-blue-100 rounded-full mb-4">
                        <i class="fas fa-search text-blue-600" aria-hidden="true"></i>
                    </div>
                    <h3 class="text-lg font-semibold text-blue-800 mb-2">SEO Optimized</h3>
                    <p class="text-blue-700 text-sm">Creates search engine friendly URLs that improve your site's SEO ranking and visibility.</p>
                </div>

                <div class="text-center p-6 bg-green-50 rounded-lg">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-green-100 rounded-full mb-4">
                        <i class="fas fa-rocket text-green-600" aria-hidden="true"></i>
                    </div>
                    <h3 class="text-lg font-semibold text-green-800 mb-2">Instant Results</h3>
                    <p class="text-green-700 text-sm">Convert text to slugs instantly with real-time preview and multiple formatting options.</p>
                </div>

                <div class="text-center p-6 bg-purple-50 rounded-lg">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-purple-100 rounded-full mb-4">
                        <i class="fas fa-cogs text-purple-600" aria-hidden="true"></i>
                    </div>
                    <h3 class="text-lg font-semibold text-purple-800 mb-2">Customizable</h3>
                    <p class="text-purple-700 text-sm">Multiple options for separators, case handling, length limits, and special character processing.</p>
                </div>

                <div class="text-center p-6 bg-red-50 rounded-lg">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-red-100 rounded-full mb-4">
                        <i class="fas fa-shield-alt text-red-600" aria-hidden="true"></i>
                    </div>
                    <h3 class="text-lg font-semibold text-red-800 mb-2">Privacy Safe</h3>
                    <p class="text-red-700 text-sm">All processing happens in your browser. No data is sent to our servers or stored anywhere.</p>
                </div>

                <div class="text-center p-6 bg-yellow-50 rounded-lg">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-yellow-100 rounded-full mb-4">
                        <i class="fas fa-mobile-alt text-yellow-600" aria-hidden="true"></i>
                    </div>
                    <h3 class="text-lg font-semibold text-yellow-800 mb-2">Mobile Friendly</h3>
                    <p class="text-yellow-700 text-sm">Responsive design works perfectly on desktop, tablet, and mobile devices.</p>
                </div>

                <div class="text-center p-6 bg-indigo-50 rounded-lg">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-indigo-100 rounded-full mb-4">
                        <i class="fas fa-code text-indigo-600" aria-hidden="true"></i>
                    </div>
                    <h3 class="text-lg font-semibold text-indigo-800 mb-2">Developer Ready</h3>
                    <p class="text-indigo-700 text-sm">Perfect for developers, content creators, and anyone working with URLs and web content.</p>
                </div>
            </div>
        </section>

        <!-- Use Cases -->
        <section class="bg-white rounded-xl shadow-lg p-8 mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                <i class="fas fa-users mr-3 text-blue-500" aria-hidden="true"></i>
                Use Cases & Applications
            </h2>
            
            <div class="grid md:grid-cols-2 gap-8">
                <div>
                    <h3 class="text-lg font-semibold text-gray-800 mb-4">Perfect For:</h3>
                    <ul class="space-y-2 text-gray-700">
                        <li class="flex items-center"><i class="fas fa-check text-green-500 mr-2" aria-hidden="true"></i> Blog post permalinks</li>
                        <li class="flex items-center"><i class="fas fa-check text-green-500 mr-2" aria-hidden="true"></i> Product page URLs</li>
                        <li class="flex items-center"><i class="fas fa-check text-green-500 mr-2" aria-hidden="true"></i> Article titles</li>
                        <li class="flex items-center"><i class="fas fa-check text-green-500 mr-2" aria-hidden="true"></i> Category names</li>
                        <li class="flex items-center"><i class="fas fa-check text-green-500 mr-2" aria-hidden="true"></i> File naming</li>
                        <li class="flex items-center"><i class="fas fa-check text-green-500 mr-2" aria-hidden="true"></i> Database identifiers</li>
                    </ul>
                </div>
                <div>
                    <h3 class="text-lg font-semibold text-gray-800 mb-4">Industries:</h3>
                    <ul class="space-y-2 text-gray-700">
                        <li class="flex items-center"><i class="fas fa-briefcase text-blue-500 mr-2" aria-hidden="true"></i> Web Development</li>
                        <li class="flex items-center"><i class="fas fa-briefcase text-blue-500 mr-2" aria-hidden="true"></i> Content Marketing</li>
                        <li class="flex items-center"><i class="fas fa-briefcase text-blue-500 mr-2" aria-hidden="true"></i> E-commerce</li>
                        <li class="flex items-center"><i class="fas fa-briefcase text-blue-500 mr-2" aria-hidden="true"></i> Digital Publishing</li>
                        <li class="flex items-center"><i class="fas fa-briefcase text-blue-500 mr-2" aria-hidden="true"></i> SEO Agencies</li>
                        <li class="flex items-center"><i class="fas fa-briefcase text-blue-500 mr-2" aria-hidden="true"></i> Blogging Platforms</li>
                    </ul>
                </div>
            </div>
        </section>

        <!-- FAQs Section -->
        <section class="bg-white rounded-xl shadow-lg p-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                <i class="fas fa-question-circle mr-3 text-purple-600" aria-hidden="true"></i>
                Frequently Asked Questions
            </h2>
            
            <div class="space-y-6">
                <div class="border-l-4 border-blue-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">What is a URL slug?</h3>
                    <p class="text-gray-600">A URL slug is the part of a URL that identifies a particular page on a website in an easy-to-read form. It typically contains only lowercase letters, numbers, and hyphens, making it SEO-friendly and user-readable.</p>
                </div>
                
                <div class="border-l-4 border-green-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">How does a text to slug converter work?</h3>
                    <p class="text-gray-600">A text to slug converter transforms regular text into URL-friendly format by converting to lowercase, replacing spaces with hyphens, removing special characters, and ensuring the result follows URL best practices for SEO optimization.</p>
                </div>
                
                <div class="border-l-4 border-orange-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">Why are URL slugs important for SEO?</h3>
                    <p class="text-gray-600">URL slugs are important for SEO because they help search engines understand page content, improve user experience with readable URLs, and can include relevant keywords that boost search rankings.</p>
                </div>
                
                <div class="border-l-4 border-purple-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">What characters are allowed in URL slugs?</h3>
                    <p class="text-gray-600">URL slugs should typically contain only lowercase letters (a-z), numbers (0-9), and hyphens (-). Avoid spaces, special characters, and uppercase letters for maximum compatibility and SEO benefits.</p>
                </div>
                
                <div class="border-l-4 border-red-500 pl-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-2">Can I customize slug generation options?</h3>
                    <p class="text-gray-600">Yes, our text to slug converter offers various customization options including separator choice (hyphens or underscores), case conversion, character limits, and special character handling preferences.</p>
                </div>
            </div>
        </section>
    </main>

    <?php include 'footer.php'; ?>

    <!-- JavaScript -->
    <script>
        class TextToSlugConverter {
            constructor() {
                this.slugHistory = [];
                this.init();
            }

            init() {
                this.bindEvents();
                this.updateCharCount();
            }

            bindEvents() {
                document.getElementById('inputText').addEventListener('input', () => {
                    this.updateCharCount();
                    this.autoConvert();
                });
                document.getElementById('convertBtn').addEventListener('click', () => this.convertToSlug());
                document.getElementById('clearBtn').addEventListener('click', () => this.clearInput());
                document.getElementById('copyBtn').addEventListener('click', () => this.copySlug());
                document.getElementById('newSlugBtn')?.addEventListener('click', () => this.newSlug());
                document.getElementById('downloadBtn')?.addEventListener('click', () => this.downloadHistory());
                document.getElementById('shareBtn')?.addEventListener('click', () => this.shareTool());

                // Option cards interaction
                document.querySelectorAll('.option-card').forEach(card => {
                    card.addEventListener('click', () => {
                        if (card.dataset.option === 'separator') {
                            card.classList.add('active');
                            document.querySelectorAll('[data-option="separator"]').forEach(other => {
                                if (other !== card) other.classList.remove('active');
                            });
                        }
                        this.autoConvert();
                    });
                });

                // Settings change listeners
                document.querySelectorAll('input[type="radio"], input[type="checkbox"], select').forEach(input => {
                    input.addEventListener('change', () => this.autoConvert());
                });

                // Example cards
                document.querySelectorAll('.example-card').forEach(card => {
                    card.addEventListener('click', () => {
                        document.getElementById('inputText').value = card.dataset.example;
                        this.updateCharCount();
                        this.convertToSlug();
                    });
                });
            }

            updateCharCount() {
                const text = document.getElementById('inputText').value;
                document.getElementById('charCount').textContent = `${text.length} characters`;
            }

            autoConvert() {
                const text = document.getElementById('inputText').value.trim();
                if (text.length > 0) {
                    this.convertToSlug(false);
                }
            }

            convertToSlug(showResults = true) {
                const text = document.getElementById('inputText').value.trim();
                
                if (!text) {
                    alert('Please enter some text to convert.');
                    return;
                }

                const options = this.getOptions();
                const slug = this.generateSlug(text, options);

                if (showResults) {
                    this.displayResults(slug, text);
                    this.addToHistory(text, slug, options);
                }

                return slug;
            }

            getOptions() {
                return {
                    separator: document.querySelector('input[name="separator"]:checked').value,
                    preserveCase: document.getElementById('preserveCase').checked,
                    maxLength: parseInt(document.getElementById('maxLength').value),
                    removeNumbers: document.getElementById('removeNumbers').checked,
                    strictMode: document.getElementById('strictMode').checked
                };
            }

            generateSlug(text, options) {
                let slug = text;

                // Remove HTML tags if any
                slug = slug.replace(/<[^>]*>/g, '');

                // Handle special characters and accents
                slug = this.normalizeText(slug);

                // Remove numbers if requested
                if (options.removeNumbers) {
                    slug = slug.replace(/\d+/g, '');
                }

                // Convert to lowercase unless preserving case
                if (!options.preserveCase) {
                    slug = slug.toLowerCase();
                }

                // Replace spaces and multiple whitespace with separator
                const separator = options.separator === 'underscore' ? '_' : '-';
                slug = slug.replace(/\s+/g, separator);

                // Remove special characters (keep only letters, numbers, separators)
                if (options.strictMode) {
                    slug = slug.replace(/[^a-zA-Z0-9\-_]/g, '');
                } else {
                    slug = slug.replace(/[^\w\-_]/g, '');
                }

                // Replace multiple separators with single separator
                const separatorRegex = new RegExp(`[${separator}]+`, 'g');
                slug = slug.replace(separatorRegex, separator);

                // Remove leading and trailing separators
                slug = slug.replace(new RegExp(`^[${separator}]+|[${separator}]+$`, 'g'), '');

                // Apply length limit
                if (options.maxLength > 0 && slug.length > options.maxLength) {
                    slug = slug.substring(0, options.maxLength);
                    // Remove trailing separator if cut off
                    slug = slug.replace(new RegExp(`[${separator}]+$`), '');
                }

                return slug || 'untitled-slug';
            }

            normalizeText(text) {
                // Normalize accented characters
                return text
                    .normalize('NFD')
                    .replace(/[\u0300-\u036f]/g, '')
                    // Replace common special characters
                    .replace(/[&]/g, 'and')
                    .replace(/[%]/g, 'percent')
                    .replace(/[@]/g, 'at')
                    .replace(/[+]/g, 'plus')
                    .replace(/[#]/g, 'hash')
                    .replace(/[\$]/g, 'dollar');
            }

            displayResults(slug, originalText) {
                const resultsSection = document.getElementById('resultsSection');
                resultsSection.classList.remove('hidden');

                // Update slug result
                document.getElementById('slugResult').textContent = slug;
                document.getElementById('slugLength').textContent = slug.length;

                // Update previews
                document.getElementById('wpPreview').textContent = slug;
                document.getElementById('blogPreview').textContent = slug;
                document.getElementById('productPreview').textContent = slug;

                // Scroll to results
                resultsSection.scrollIntoView({ behavior: 'smooth' });
            }

            addToHistory(originalText, slug, options) {
                const historyItem = {
                    timestamp: new Date(),
                    original: originalText,
                    slug: slug,
                    options: options
                };
                
                this.slugHistory.unshift(historyItem);
                // Keep only last 50 items
                if (this.slugHistory.length > 50) {
                    this.slugHistory = this.slugHistory.slice(0, 50);
                }
            }

            copySlug() {
                const slug = document.getElementById('slugResult').textContent;
                
                if (navigator.clipboard) {
                    navigator.clipboard.writeText(slug).then(() => {
                        this.showCopyStatus();
                    });
                } else {
                    // Fallback for older browsers
                    const textArea = document.createElement('textarea');
                    textArea.value = slug;
                    document.body.appendChild(textArea);
                    textArea.select();
                    document.execCommand('copy');
                    document.body.removeChild(textArea);
                    this.showCopyStatus();
                }
            }

            showCopyStatus() {
                const statusElement = document.getElementById('copyStatus');
                statusElement.classList.remove('hidden');
                setTimeout(() => {
                    statusElement.classList.add('hidden');
                }, 2000);
            }

            clearInput() {
                document.getElementById('inputText').value = '';
                document.getElementById('resultsSection').classList.add('hidden');
                this.updateCharCount();
            }

            newSlug() {
                this.clearInput();
                document.getElementById('inputText').focus();
            }

            downloadHistory() {
                if (this.slugHistory.length === 0) {
                    alert('No slug history to download.');
                    return;
                }

                const content = this.slugHistory.map(item => {
                    return `${item.timestamp.toISOString()}\t${item.original}\t${item.slug}`;
                }).join('\n');

                const blob = new Blob([`Timestamp\tOriginal Text\tGenerated Slug\n${content}`], { type: 'text/plain' });
                const url = URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.href = url;
                a.download = `slug-history-${Date.now()}.txt`;
                a.click();
                URL.revokeObjectURL(url);
            }

            shareTool() {
                if (navigator.share) {
                    navigator.share({
                        title: 'Text to Slug Converter',
                        text: 'Convert text to URL-friendly slugs instantly!',
                        url: window.location.href
                    });
                } else {
                    // Fallback: copy URL to clipboard
                    navigator.clipboard.writeText(window.location.href).then(() => {
                        alert('Tool URL copied to clipboard!');
                    });
                }
            }
        }

        // Initialize converter when page loads
        document.addEventListener('DOMContentLoaded', () => {
            new TextToSlugConverter();
        });

        // Add scroll animations
        document.addEventListener('DOMContentLoaded', () => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('fade-in');
                    }
                });
            });

            document.querySelectorAll('section').forEach(el => {
                observer.observe(el);
            });
        });
    </script>
</body>
</html>